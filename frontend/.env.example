# Open-NueChat Frontend Environment Variables
# NOTE: These are NOT needed in production.
# The frontend auto-detects API and WebSocket URLs from window.location.
# These are only for local development outside Docker.

# For local dev only:
# VITE_API_URL=http://localhost:8000/api
