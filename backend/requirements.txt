# FastAPI and Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
websockets>=12.0

# Database
sqlalchemy>=2.0.25
alembic>=1.13.1
aiosqlite>=0.19.0

# Authentication
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
authlib>=1.3.0
httpx>=0.26.0

# LLM Integration - OpenAI Compatible API
openai>=1.12.0

# Token counting
tiktoken>=0.5.2

# RAG - Embeddings (ROCm compatible via PyTorch ROCm)
# Note: PyTorch is pre-installed in ROCm base image, don't override
sentence-transformers>=3.4.0
numpy>=1.26.0

# FAISS GPU (ROCm) - built from source in Dockerfile
# See: https://github.com/faiss-wheels/faiss-wheels

# Utilities
python-dotenv>=1.0.0
pydantic>=2.5.3
pydantic-settings>=2.1.0
aiofiles>=23.2.1
pillow>=10.2.0
python-magic>=0.4.27

# Testing
pytest>=7.0.0,<9.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
email-validator>=2.0.0
pyyaml>=6.0
cryptography>=41.0.0
fastmcp>=2.0.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Document conversion
markdown-pdf>=1.10

# File locking for persistent queues
filelock>=3.12.0

# TTS (Text-to-Speech) - RPC client to Kokoro microservice
# (uses httpx from Authentication section)

# STT (Speech-to-Text) - Whisper
transformers>=4.36.0
accelerate>=0.25.0
scipy>=1.11.0
soundfile>=0.12.1
